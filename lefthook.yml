pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: mise exec -- corepack yarn eslint {staged_files}
    typecheck:
      glob: '*.{ts,tsx}'
      run: mise exec -- corepack yarn typecheck
    prettier:
      glob: '*.{js,jsx,ts,tsx,json,md,yml,yaml}'
      run: mise exec -- corepack yarn prettier --check {staged_files}
    test:
      glob: '*.{ts,tsx}'
      run: mise exec -- corepack yarn test --findRelatedTests {staged_files} --passWithNoTests

pre-push:
  parallel: true
  commands:
    test-coverage:
      run: mise exec -- corepack yarn test --coverage --silent
    build:
      run: mise exec -- corepack yarn prepare

commit-msg:
  commands:
    commitlint:
      run: mise exec -- corepack yarn commitlint --edit {1}
