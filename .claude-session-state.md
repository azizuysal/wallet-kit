# Claude Session State - wallet-kit Release Issue

## Current Task

Fixing release-it changelog generation issues for v1.0.0 release

## Problem

1. Changelog entries are being duplicated instead of replaced
2. Commits are not being grouped by type (Features, Bug Fixes, etc.)
3. Two conflicting versions of conventional-changelog-conventionalcommits:
   - v6.1.0 from @commitlint/config-conventional
   - v8.0.0 from @release-it/conventional-changelog

## Last Action Attempted

Was about to run: `yarn add -D release-it@^18.0.0 @release-it/conventional-changelog@^10.0.0`

- Purpose: Update to compatible versions to resolve dependency conflict
- User interrupted before execution

## Current State

- Package version: 0.2.1 (in package.json)
- Git: Clean working directory
- CHANGELOG.md: Contains only historical releases (0.1.0, 0.2.0, 0.2.1)
- release-it config: Simplified to use "conventionalcommits" preset as string

## Release-it Configuration (current)

```json
"plugins": {
  "@release-it/conventional-changelog": {
    "preset": "conventionalcommits",
    "infile": "CHANGELOG.md"
  }
}
```

## Dependency Versions (current)

- release-it: ^17.10.0
- @release-it/conventional-changelog: ^9.0.2 (actually 9.0.3 installed)

## Next Steps

1. Update release-it and @release-it/conventional-changelog to compatible versions
2. Ensure only one version of conventional-changelog-conventionalcommits is used
3. Test changelog generation works properly with grouping
4. Release version 1.0.0

## User's Requirements

- No manual intervention in changelog generation
- Proper grouping of commits by type
- No duplication of entries
- Only release-it should write to CHANGELOG.md
